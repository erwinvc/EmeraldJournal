<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EmeraldJournal - Journaling tooling</title>
    <base href="~/" />

    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
    <link href="_content/MudBlazor/MudBlazor.min.css" rel="stylesheet" />
    <link href="_content/MudBlazor.Markdown/MudBlazor.Markdown.min.css" rel="stylesheet" />
    <link rel="manifest" href="~/manifest.json" />
    <meta name="theme-color" content="#27272f" />
    <link rel="apple-touch-icon" href="~/icon-192.png" />
    <!-- Prevent pinch-and-zoom / double-tap zoom on iOS -->
    <meta name="viewport"
          content="width=device-width,
                 initial-scale=1,
                 maximum-scale=1,
                 user-scalable=no" />

    <!-- Tell (most) browsers not to trigger a double-tap → zoom -->
    <style>
        html, body {
            /* “manipulation” allows pans & taps but disables double-tap zoom */
            touch-action: manipulation;
            /* On iOS Safari specifically, texts smaller than 16px will still auto-zoom on inputs.
                         If you still see zooming when focusing a <input>, bump its font-size to ≥16px. */
            -webkit-text-size-adjust: 100%;
        }
    </style>
    <link href="css/site.css" rel="stylesheet" />
</head>
<body>
    @RenderBody()
    <div id="blazor-error-ui"
         class="mud-alert mud-alert-filled mud-alert-severity-error"
         style="display:none; position:fixed; bottom:16px; left:16px; right:16px; z-index:9999;">

        <div class="mud-alert-content">
            <environment include="Staging,Production">
                An error has occurred. The application may stop responding until you reload.
            </environment>
            <environment include="Development">
                An unhandled exception has occurred. See browser dev tools for details.
            </environment>
        </div>

        <div class="mud-alert-actions">
            <a href="" class="reload mud-button mud-button-filled mud-elevation-0 me-2">
                Reload
            </a>
            <button type="button"
                    class="dismiss mud-icon-button"
                    aria-label="Dismiss">
                <span class="material-icons">close</span>
            </button>
        </div>
    </div>

    <script src="_content/MudBlazor/MudBlazor.min.js"></script>
    <script src="_content/MudBlazor.Markdown/MudBlazor.Markdown.min.js"></script>
    <script src="_framework/blazor.server.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Sortable/1.15.6/Sortable.min.js"></script>
    <script src="~/js/download.js"></script>
    <script src="~/js/auth.js"></script>
</body>
</html>